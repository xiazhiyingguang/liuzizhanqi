# 六子战棋局域网联机指南

## 一条命令启动

在作为主机的电脑上进入项目目录，首次运行先安装依赖：

```bash
npm install
```

根目录安装会自动安装联机服务器依赖。

以后每次联机只需要在项目根目录运行：

```bash
npm run lan
```

命令会先构建网页，再启动局域网服务器。终端会显示类似地址：

```text
本机访问: http://127.0.0.1:8787
局域网访问: http://192.168.1.10:8787
```

主机和另一台电脑都打开同一个“局域网访问”地址即可，不需要分别配置前端和后端端口。

## 对战流程

1. 两台电脑连接同一个路由器或局域网。
2. 主机运行 `npm run lan`，把终端显示的局域网网址发给另一位玩家。
3. 双方浏览器打开同一个网址，进入“联机对战”。
4. 双方输入约定好的同一个四位数字（例如 `1357`），点击“进入房间”。
5. 第一位玩家会自动等待，第二位玩家输入相同数字后立即配对，无需创建房间或填写名称。
6. 双方各自选将、布阵，然后进入实时对战。

浏览器会自动生成一个本地棋友身份，并记录最近对战过的玩家。双方进入联机大厅后，如果最近的对手当前在线，也可以直接点击“邀请”；对方接受后会自动进入同一房间。

自动身份和最近对战记录只保存在当前浏览器中。清除浏览器站点数据或更换浏览器后，会生成新的身份和记录。

## 开发模式

开发时可分别运行：

```bash
npm run server
npm run dev
```

开发页面打开在 `http://主机IP:3000` 时，客户端会自动连接同一主机的 `8787` 端口，因此不会再把 `3000` 错当成联机服务器。也可以直接使用生产模式的 `http://主机IP:8787`。

## 无法访问时

- 确认两台电脑处于同一局域网，且没有启用访客网络隔离。
- Windows 首次启动时，允许 Node.js 通过“专用网络”防火墙。
- 只需要放行 TCP 端口 `8787`；开发模式还需要放行 `3000`。
- 在另一台电脑访问 `http://主机IP:8787/api/health`，应看到服务器状态 JSON。
- 房间保存在主机内存中，主机退出或服务器重启后，当前房间会关闭。
- 当前版本在玩家主动离开或网络中断时会关闭房间，需要重新建房。
- “在线棋友”指已经打开联机大厅并连接到当前服务器的玩家；直接邀请需要对方保持页面在线。

## 已实现的联机约束

- 每个房间只允许两名玩家。
- 服务器校验选将数量、部署区域、部署数量和当前行动方。
- 选将与布阵操作按服务器接收顺序合并，避免双方同时操作时互相覆盖。
- 战斗阶段由行动方提交执行后的权威状态，随机技能不会在两台电脑上重复计算。
- 非当前行动方不能移动或释放技能。
- 玩家离开、断线或房间超时后，服务器会通知对手并清理房间。
