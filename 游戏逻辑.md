# 六子战棋 - 深度游戏逻辑与开发百科 (v2.0)

本文档是为大模型及开发者准备的《六子战棋》全量技术指南。它详细描述了游戏从底层架构到高层战斗机制的所有逻辑，旨在支持复杂角色开发与系统维护。

---

## 1. 系统架构与技术实现

### 1.1 状态驱动模型 (Zustand)
游戏状态存储在 [game-store.ts](file:///d:/code/Game/six-chess-battle/src/store/game-store.ts) 中。
- **不可变性**: 每次状态更新必须通过 `set((state) => ({ ... }))`，且必须对引用类型（如 `board` 数组）进行深拷贝。
- **动作响应**: 所有的 UI 交互最终调用 Store 中的 Action。

### 1.2 联机同步协议
- **Socket.IO 事件**: 
  - `player-action`: 客户端发送动作意图（如 `move`, `skill`）。
  - `action-broadcast`: 服务器验证通过后，将动作广播给房间内所有玩家。
  - `game-state-update`: 服务器定期发送完整的 `GameState` 快照，用于校准数据。
- **本地预测与校验**: 客户端在收到 `action-broadcast` 后，通过 [useOnlineSync.ts](file:///d:/code/Game/six-chess-battle/src/hooks/useOnlineSync.ts) 调用 Store 动作同步显示。

---

## 2. 战斗核心逻辑 (Combat Core)

### 2.1 伤害计算全流程
见 [damage-calculator.ts](file:///d:/code/Game/six-chess-battle/src/core/damage-calculator.ts)。
1. **基础值确定**: 技能基础伤害 + 攻击力系数（若 `scalesWithAttack: true`）。
2. **暴击判定**: 检查 `canCrit` 标记。计算来源英雄的暴击率（基础+BUFF+特殊被动如孙悟空的“灵犀”）。
3. **防御减免**: `最终伤害 = 原始伤害 * (1 - 目标防御力%)`。
4. **护盾抵扣**: 伤害先扣除 `hero.shield`，剩余部分扣除 `hero.currentHp`。
5. **吸血触发**: 若技能带吸血或英雄带吸血 BUFF，按 `实际生命扣除量 * 吸血率` 回复来源。
6. **反击检查**: 计算后检查目标是否有反击被动（如震霄的“金银错”），若有则反向触发一次非基础攻击。

### 2.2 死亡判定机制
- **触发点**: 任何生命值变动后。
- **处理逻辑**:
  - 标记 `state = HeroState.DEAD`。
  - 将 `board[row][col]` 置为 `null`。
  - **分身清理**: 若该英雄是分身来源（如孙悟空/镜），则遍历棋盘清除所有关联的分身。
  - **天威触发**: 检查击杀者是否拥有击杀触发型天威（如夜枭、孙悟空）。

---

## 3. 技能与范围系统 (Skill System)

### 3.1 范围判定算法 (MovementSystem)
- **曼哈顿距离**: `dist = abs(r1-r2) + abs(c1-c2)`。用于普攻和单体技能。
- **区域检测**: `getAreaPositions(center, size)` 返回中心点周围 NxN 的所有合法坐标。
- **合法性过滤**: 技能释放前需检查目标位置是否符合 `targetType`（`enemy`, `ally`, `empty` 等）。

### 3.2 技能执行结果 (SkillExecuteResult)
技能执行不仅产生伤害，还必须返回：
- `success`: 是否执行成功（如位置被占则不成功）。
- `log`: 战斗日志字符串数组。
- `effectsApplied`: 产生的 BUFF/DEBUFF 列表。

---

## 4. 特殊机制详解 (Deep Gimmicks)

### 4.1 孙悟空体系
- **灵犀被动**: 这是一个跨单位的全局逻辑。分身阵亡 -> 本体获得“灵犀”层数 -> 提升本体及所有分身的暴击率。
- **分身特性**: 
  - `moveRange: 0` (通常不可移动)。
  - `hasActedThisTurn: true` (召唤出来即视为已行动)。

### 4.2 寒天/冰冻体系
- **堆叠逻辑**: `cold` 层数在 [effect-manager.ts](file:///d:/code/Game/six-chess-battle/src/core/effect-manager.ts) 中通过 `stackCount` 管理。
- **状态转换**: 达到 3 层时，`DamageCalculator` 会自动移除 `cold` 并添加 `stun` 效果。

### 4.3 镜的分身与位移
- **镜像生成**: 在棋盘对称位置创建单位。
- **交换逻辑**: 交换本体与分身的 `position` 坐标，并更新 `board` 引用。

---

## 5. 开发建议与避坑指南

### 5.1 编写新角色的数据结构
```typescript
// 在 heroes.ts 中定义
export const myNewHeroTemplate = {
    name: '新英雄',
    maxHp: 45,
    moveRange: 2,
    baseAttack: 0,
    passiveId: 'my_passive',
    // ...技能ID
};
```

### 5.2 必须遵守的原则
1. **不要直接修改属性**: 永远不要做 `hero.currentHp -= 10`。请使用 `DamageCalculator.applyDamage`。
2. **被动触发位置**: 所有的被动技能逻辑必须在 `DamageCalculator`（伤害类）、`MovementSystem`（位移类）或 `GameEngine.endHeroAction`（回合类）中显式调用。
3. **分身识别**: 永远通过 `hero.counters?.[__isClone] === 1` 来识别召唤物。
4. **联机兼容性**: 任何 `set` 状态的行为，如果是在玩家主动操作下发生的，必须确保有对应的 `sendOnlineActionIfNeeded`。

---

## 6. 进阶扩展接口
- **EffectManager**: 用于添加/移除/查询所有状态效果。
- **SkillSystem**: 用于查询技能合法目标范围。
- **BattleLog**: 所有的交互都应通过 `get().addLog` 产生反馈。
